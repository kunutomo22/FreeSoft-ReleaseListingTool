$LogLevel = "Warning"               #この設定値以上のログが出力されます。
						            # Error: エラーログ(エラーログのみ出力)
						            # Warning: 警告ログ(警告ログとエラーログのみ出力)				※ デフォルト
						            # Information: 情報ログ以上を出力する(デバッグログ以外全て出力)
						            # None: 全てのログを出力する(デバッグログも出力)

$Popup = $true                      #ポップアップを表示するかどうかの設定です。$trueにするとポップアップが表示されます。※一部のユーザの応答が必要なポップアップはこの設定に関わらず表示されます。
						            #trueがデフォルトです。

$OutType = "File"                   #リストの出力形式を設定します。
						            # File: ファイルに出力      ※ デフォルト
						            #
						            # Popup: ポップアップに出力 ※1 $Popup = $falseとするとリスト出力時のみポップアップが表示されます。
						            #                           ※2 制限:内容の説明は末尾が省略され完全に表示されません。内容の説明を完全に確認するには$OutType = "File"に設定しファイルを確認してください。
						            #
						            # Console: コンソールに出力 ※ ログにも出力されます。

$TimeFormat = "yyyy/MM/dd HH:mm:ss" #リストに記載される日時フォーマット

$PopupSizeX = 2000		            #リストの出力ポップアップの横のサイズを設定します。
$PopupSizeY = 980		            #リストの出力ポップアップの縦のサイズを設定します。
						            #※リストの出力ポップアップのみ有効な設定です。OutType = "Popup"の時のみ有効です。

$Encoding = "UTF8"                  #ファイルのエンコーディングを設定します。(OS標準の文字コードにするにはDefault)※ OutType = "File"の時のみ有効です。

$FileExtension = "csv"              #出力するファイルの拡張子を設定します。※ OutType = "File"の時のみ有効です。
						            # csv: CSV形式で出力     ※1 デフォルト
						            #                        ※2 想定外の拡張子を設定した場合はファイル名を想定外の拡張子にしCSV形式で出力されます。
						            #
						            # txt: TXT形式で出力     ※ リリース履歴そのまま出力します。
						            #
						            # json: JSON形式で出力
						            #
						            # xml: XML形式で出力
						            #
						            # html: HTML形式で出力
						            #
						            # md: Markdown形式で出力
#####################################
#FileExtensionをhtmlに設定した場合のスタイルシートです。必要に応じて変更してください。
#先頭の「$css = @"」と末尾の「"@」以外を変更しないでください。

$css = @"
<style>
table, th, td {
    border: 1px solid #444;
    border-collapse: collapse;
    padding: 6px;
}
th {
    background-color: #eee;
}
</style>
"@